// =====================================================
// Prisma base schema configuration
// Contains generator & datasource definitions.
// =====================================================

generator client {
  provider = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "mysql"
}
